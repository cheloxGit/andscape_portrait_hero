import 'package:flutter/material.dart';

class Jupyter extends StatelessWidget {
  const Jupyter({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Jupyter')),
    );
  }
}
